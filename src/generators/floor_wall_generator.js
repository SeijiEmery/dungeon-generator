
export function generateFloorsAndWalls(params) {
    let { width, height, values } = params;

    // TODO: implement this

    return {
        floors:  null,
        walls:   null,
        objects: null,
        width: width,
        height: height,
    }
}
